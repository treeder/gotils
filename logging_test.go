package gotils

import (
	"testing"
)

func TestContextLogging(t *testing.T) {
	// ctx := context.Background()
	// err := errors.New("error 1")
	// t.Log(ErrString(err))
	// err = Errorf(ctx, "error 2: %v", err)
	// t.Log(ErrString(err))
	// ctx = With(ctx, "abc", 123)
	// err = Errorf(ctx, "error3: %v", err)
	// t.Log(ErrString(err))
	// t.Error(errors.New("end"))
}
